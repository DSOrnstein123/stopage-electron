DeckPaths:
  get:
      summary: Get deck
      responses:
        "200":
          description: Get deck
          content:
            application/json:
              schema:
                $ref: "./components.yaml#/components/schemas/Deck"